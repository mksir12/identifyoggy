services:
  - type: web
    name: shazamsearch
    env: python
    buildCommand: "apt update && apt install ffmpeg -y && pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host=0.0.0.0 --port=10000"
    plan: free
